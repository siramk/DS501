Sorted List of document ids matching each query:

	'IIT and Computer': [27, 26]
	'IIT and mhrd': [33, 21, 22]
	'Bhilai and Raipur and bilaspur': [56, 41, 48]
	


TF-IDF Scheme used:

Term frequency is calculated as:
TF(t,d) = 0.5 + (0.5 * freq(t, d) / max{freq(t, d) | w is in d})

Inverse document frequency is caluclated as:
iDF(t) = log(N/DF(t)) where N = total number of documents and DF(t) = number of documents in which t occurs